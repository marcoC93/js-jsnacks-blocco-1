//definisco una variabile che andrá a definire quello che sará il limte del contatore puó essere statica o definita dall'utente
var n = parseInt(prompt("scrivi un numero"));

for (i=0 ; i < n; i++ ) {

    //  
    potenza = i * i * i ;
    console.log(potenza);
}
